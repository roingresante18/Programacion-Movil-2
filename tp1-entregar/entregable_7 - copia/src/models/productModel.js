class product {
    constructor(id, nombre, detalle, precio, stock){
     this.id = id;
     this.nombre = nombre;
     this.detalle = detalle;
     this.precio = precio;
     this.stock = stock;
    }
}